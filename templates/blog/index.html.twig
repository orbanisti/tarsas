{% extends 'base.html.twig' %}

{% block body_id 'blog_index' %}

{% block body %}
    <div class="container  pb-5">
        <div class="row ">
            <div class="col-md-12">
                <h1 class=" ml-4 text-left text-black ">
                    {{ 'Blog'|trans }}
                </h1>
            </div>
        </div>
        <div class=" mb-5 flex-row" style="">
            {% for post in paginator.results %}
                <div class="col-md-4 p-4">
                    {{ include('frontend/embed/jobPreview.html.twig',{title:post.title,description:post.lead,url:path('blog_post',{url:post.url})}) }}
                </div>
            {% else %}
                <div class="well">{{ 'post.no_posts_found'|trans }}</div>
            {% endfor %}


        </div>
        <div class="row mt-5">
            <div class="col-md-12">
                {% if paginator.hasToPaginate %}
                    <div class="navigation text-center">
                        <ul class="pagination mt-5">
                            {% if paginator.hasPreviousPage %}
                                <li class="prev"><a href="{{ path('blog_index_paginated', {page: paginator.previousPage}) }}" rel="previous"><i
                                                class="fa fw fa-long-arrow-left"></i> {{ 'Előző'|trans }}</a></li>
                            {% else %}
                                <li class="prev disabled"><span><i class="fa fw fa-arrow-left"></i> {{ 'Előző'|trans }}</span></li>
                            {% endif %}

                            {% for i in 1..paginator.lastPage %}
                                {% if i == paginator.currentPage %}
                                    <li class="active"><span class="bg-primary p-2 m-1 text-white">{{ i }} <span
                                                    class="sr-only">{{ 'Jelenlegi'|trans }}</span></span>
                                    </li>
                                {% else %}
                                    <li><a class="p-2 m-1" href="{{ path('blog_index_paginated', {page: i}) }}">{{ i }}</a></li>
                                {% endif %}
                            {% endfor %}

                            {% if paginator.hasNextPage %}
                                <li class="next"><a href="{{ path('blog_index_paginated', {page: paginator.nextPage}) }}"
                                                    rel="next">{{ 'Következő'|trans }} <i class="fa fw fa-arrow-right"></i></a></li>
                            {% else %}
                                <li class="next disabled"><span>{{ 'Következő'|trans }} <i class="fa fw fa-arrow-right"></i></span></li>
                            {% endif %}
                        </ul>
                    </div>
                {% endif %}
            </div>
        </div>

    </div>

{% endblock %}
