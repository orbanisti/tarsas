{% extends 'base.html.twig' %}

{% block body %}

    <div class="flex-container">
        <div class="flex-slide accounting" onclick="redirectToPath('{{ path('accounting') }}')">
            <a href="{{ path('accounting') }}" style="text-decoration: none !important;">
                <div class="flex-title flex-title-home">{{ 'Könyvelés'|trans }}</div>
            </a>

            {# <div class="flex-about flex-about-home"><p>Click here to navigate to the home section of the website</p></div> #}
        </div>

        <div class="flex-slide renewables" onclick="redirectToPath('{{ path('renewables') }}')">
            <a href="{{ path('renewables') }}" style="text-decoration: none !important;">
                <div class="flex-title">{{ 'Megújuló energia'|trans }}</div>
            </a>
        </div>
        <div class="flex-slide itsolutions" onclick="redirectToPath('{{ path('it-solutions') }}')">
            <a href="{{ path('it-solutions') }}" style="text-decoration: none !important;">
                <div class="flex-title">{{ 'IT Megoldások'|trans }}</div>
            </a>
        </div>
    </div>
    <style>
        .accounting {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url({{asset('build/images/accounting.png')}});
        }

        .renewables {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url({{asset('build/images/renewables.png')}});
        }

        .itsolutions {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url({{asset('build/images/it-solutions.png')}});
        }
    </style>
    <style>

        .accounting:hover {
            background: linear-gradient(rgba(34, 70, 148, 0.5), rgba(34, 70, 148, 0.5)), url({{asset('build/images/accounting.png')}});
        }

        .renewables:hover {
            background: linear-gradient(rgba(34, 70, 148, 0.5), rgba(34, 70, 148, 0.5)), url({{asset('build/images/renewables.png')}});
        }

        .itsolutions:hover {
            background: linear-gradient(rgba(34, 70, 148, 0.5), rgba(34, 70, 148, 0.5)), url({{asset('build/images/it-solutions.png')}});
        }
    </style>
{% endblock %}

{% block javascripts %}
    <script>
        (function () {
            // $('.flex-container').waitForImages(function () {
            //     $('.spinner').fadeOut();
            // }, $.noop, true);

            $(".flex-slide").each(function () {
                $(this).hover(function () {
                    $(this).find('.flex-title').css({
                        top: '10%'
                    });

                }, function () {
                    $(this).find('.flex-title').css({
                        top: '30%'
                    });

                })
            });
        })();
    </script>
{% endblock %}

