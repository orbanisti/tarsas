<ul class="navbar-nav ml-auto">
    {% block header_navigation_links %}
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('accounting') }}">
                {{ 'Könyvelés'|trans }}
            </a>
        </li>
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('renewables') }}">
                {{ 'Renewables'|trans }}
            </a>
        </li>
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('it-solutions') }}">
                {{ 'IT Solutions'|trans }}
            </a>
        </li>
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('career') }}">
                {{ 'Karrier'|trans }}
            </a>
        </li>
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('about') }}">
                {{ 'Rólunk'|trans }}
            </a>
        </li>
        <li class="nav-item">
            <a class="hover-underline-animation nav-link" href="{{ path('contact') }}">
                {{ 'Kapcsolat'|trans }}
            </a>
        </li>


    {% endblock %}
    {# {% if app.user %} #}
    {# <li class="dropdown"> #}
    {# <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="user"> #}
    {# <i class="fa fa-user" aria-hidden="true"></i> #}
    {# <span class="caret"></span> #}
    {# <span class="sr-only">{{ app.user.fullname }}</span> #}
    {# </a> #}
    {# <ul class="dropdown-menu user" role="menu" aria-labelledby="user"> #}
    {# <li> #}
    {# <a href="{{ path('user_edit') }}"> #}
    {# <i class="fa fa-edit" aria-hidden="true"></i> {{ 'menu.user'|trans }} #}
    {# </a> #}
    {# </li> #}
    {# <li class="divider"></li> #}
    {# <li> #}
    {# <a href="{{ path('security_logout') }}"> #}
    {# <i class="fa fa-sign-out" aria-hidden="true"></i> {{ 'menu.logout'|trans }} #}
    {# </a> #}
    {# </li> #}
    {# </ul> #}
    {# </li> #}
    {# {% endif %} #}

    <li class="dropdown  nav-item">
        <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown" role="button" aria-expanded="false" id="locales">
            <i class="fa fa-globe" aria-hidden="true"></i>
            <span class="caret"></span>
            <span class="sr-only">{{ 'menu.choose_language'|trans }}</span>
        </a>
        <ul class="dropdown-menu d-none locales" role="menu" aria-labelledby="locales" style="left:-200%">
            {% for locale in locales() %}
            <li {% if app.request.locale == locale.code %}aria-checked="true"
                class="active dropdown-item" {% else %} class="dropdown-item" aria-checked="false"{% endif %} role="menuitem">
                {% if app.request.get('_route')!='homepage' %}
                    <a {% if app.request.locale == locale.code %}class="text-white" {% endif %}
                       href="{{ path(app.request.get('_route', 'blog_index'), app.request.get('_route_params', [])|merge({_locale: locale.code})) }}">{{ locale.name|capitalize }}
                    </a></li>

                {% else %}
                    <a {% if app.request.locale == locale.code %}class="text-white" {% endif %}
                       href="{{ path(app.request.get('_route', 'blog_index'), app.request.get('_route_params', [])|merge({_locale: locale.code})) }}{% if locale.code=='hu' %}hu{% endif %}">{{ locale.name|capitalize }}
                    </a></li>

                {% endif %}
            {% endfor %}
        </ul>
    </li>
</ul>
